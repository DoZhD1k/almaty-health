# Логика работы рекомендованных альтернатив

## Обзор

Система автоматически находит оптимальные МО для перенаправления пациентов из перегруженных больниц на основе нескольких критериев.

## 🔍 Алгоритм поиска альтернатив

### Входные параметры

```typescript
findNearbyAlternatives(
  sourceFacility, // Перегруженное МО
  allFacilities, // Все МО в системе
  (maxDistance = 15), // Максимальное расстояние (км)
  (limit = 5) // Количество альтернатив
);
```

## 📋 Этапы работы алгоритма

### Этап 1: Фильтрация МО

Система отбирает только подходящие МО по следующим критериям:

#### ✅ Критерии отбора:

1. **Исключение самого МО**

   ```typescript
   facility.id !== sourceFacility.id;
   ```

   - Нельзя перенаправить в то же самое учреждение

2. **Загруженность менее 70%**

   ```typescript
   facility.occupancy_rate_percent < 0.7;
   ```

   - Только МО с нормальной загрузкой
   - Перегруженные МО (≥70%) исключаются

3. **Наличие координат**

   ```typescript
   facility.latitude && facility.longitude;
   ```

   - Необходимо для расчета расстояния
   - МО без координат не показываются

4. **Наличие коек**
   ```typescript
   facility.beds_deployed_withdrawn_for_rep > 0;
   ```
   - МО должно иметь развернутые койки
   - Закрытые отделения исключаются

#### ❌ Что НЕ учитывается (закомментировано):

5. **Профиль коек** (опционально)
   ```typescript
   // facility.bed_profile === sourceFacility.bed_profile
   ```
   - Сейчас все МО рассматриваются равнозначно
   - Можно включить для учета специализации

### Этап 2: Расчет параметров

Для каждого подходящего МО рассчитываются:

#### 1. **Расстояние** (формула Гаверсинусов)

```typescript
distance = calculateDistance(
  source.latitude,
  source.longitude,
  target.latitude,
  target.longitude
);
```

- Точный расчет с учетом кривизны Земли
- Результат в километрах

#### 2. **Доступные койки**

```typescript
availableBeds = Math.floor(
  facility.beds_deployed_withdrawn_for_rep *
    (1 - facility.occupancy_rate_percent)
);
```

**Формула:**

- `total_beds` × (1 - `occupancy_rate`)
- Например: 100 коек × (1 - 0.5) = 50 свободных коек

#### 3. **Время в пути**

```typescript
travelTime = Math.round((distance / 40) * 60);
```

**Формула:**

- (расстояние / скорость) × 60 минут
- Средняя скорость: **40 км/ч** (городской трафик)
- Например: (10 км / 40) × 60 = 15 минут

### Этап 3: Дополнительная фильтрация

Отбираются только МО, которые:

```typescript
alt.distance <= maxDistance && alt.availableBeds > 0;
```

1. **В радиусе 15 км** от источника
2. **Имеют хотя бы 1 свободную койку**

### Этап 4: Умная сортировка (приоритизация)

МО сортируются по **комбинированному score**:

```typescript
score = (distanceWeight × distance) - (capacityWeight × availableBeds/10)
```

#### Веса приоритетов:

- **distanceWeight = 0.7** (70%) — близость важнее
- **capacityWeight = 0.3** (30%) — но и доступность важна

#### Как это работает:

**Пример 1: Близкое МО с малым количеством коек**

```
МО А: 3 км, 10 свободных коек
score = 0.7 × 3 - 0.3 × (10/10) = 2.1 - 0.3 = 1.8
```

**Пример 2: Дальнее МО с большим количеством коек**

```
МО Б: 8 км, 50 свободных коек
score = 0.7 × 8 - 0.3 × (50/10) = 5.6 - 1.5 = 4.1
```

**Результат:** МО А будет выше в списке (меньший score = выше приоритет)

### Этап 5: Ограничение выдачи

```typescript
.slice(0, limit)  // Берем топ-5
```

Возвращаются только **5 лучших** альтернатив по рассчитанному score.

## 📊 Визуализация алгоритма

```
Все МО в системе (например, 100)
        ↓
[Фильтр 1] Исключить само МО
        ↓ (99 МО)
[Фильтр 2] Только загрузка < 70%
        ↓ (например, 40 МО)
[Фильтр 3] Только с координатами
        ↓ (38 МО)
[Фильтр 4] Только с койками
        ↓ (35 МО)
[Расчет] Для каждого: distance, availableBeds, travelTime
        ↓
[Фильтр 5] Только в радиусе 15 км и с доступными койками
        ↓ (например, 12 МО)
[Сортировка] По комбинированному score (расстояние + вместимость)
        ↓
[Ограничение] Топ-5
        ↓
Результат: 5 оптимальных альтернатив
```

## 🎯 Примеры работы алгоритма

### Сценарий 1: Центр города (много вариантов)

**Перегруженное МО:** Центральная больница, 95% загруженность

**Найденные альтернативы:**

1. 🏥 Поликлиника #1 — 2.1 км, 15 мин, 8 коек (score: 1.2)
2. 🏥 Госпиталь #2 — 4.5 км, 7 мин, 25 коек (score: 2.4)
3. 🏥 Клиника #3 — 6.0 км, 9 мин, 12 коек (score: 3.8)
4. 🏥 МЦ #4 — 8.2 км, 12 мин, 30 коек (score: 4.8)
5. 🏥 Больница #5 — 10.5 км, 16 мин, 18 коек (score: 6.8)

### Сценарий 2: Окраина города (мало вариантов)

**Перегруженное МО:** Районная больница, 88% загруженность

**Найденные альтернативы:**

1. 🏥 Госпиталь — 12.5 км, 19 мин, 20 коек (score: 8.2)
2. 🏥 Клиника — 14.8 км, 22 мин, 5 коек (score: 10.2)

**Результат:** Всего 2 альтернативы (остальные за пределами 15 км или перегружены)

## 🔧 Настройка параметров

### Изменение радиуса поиска

В файле `redirection-recommendations.tsx`:

```typescript
const alternatives = findNearbyAlternatives(
  source,
  allFacilities,
  20, // ← Увеличить до 20 км
  5
);
```

### Изменение количества альтернатив

```typescript
const alternatives = findNearbyAlternatives(
  source,
  allFacilities,
  15,
  10 // ← Показывать топ-10 вместо топ-5
);
```

### Изменение весов приоритета

В файле `lib/utils/distance.ts`:

```typescript
// Приоритет расстоянию
const distanceWeight = 0.8; // ← 80% (было 70%)
const capacityWeight = 0.2; // ← 20% (было 30%)

// Или приоритет вместимости
const distanceWeight = 0.5; // ← 50%
const capacityWeight = 0.5; // ← 50% (равный вес)
```

### Изменение порога загруженности

```typescript
// Строже (только очень свободные МО)
if (facility.occupancy_rate_percent >= 0.5) return false; // < 50%

// Мягче (можно и более загруженные)
if (facility.occupancy_rate_percent >= 0.8) return false; // < 80%
```

## 📈 Метрики качества рекомендаций

### Оценка эффективности:

1. **Покрытие** — % перегруженных МО с найденными альтернативами

   ```
   Покрытие = (МО с альтернативами / Всего перегруженных МО) × 100%
   ```

2. **Среднее расстояние** — средняя дистанция до рекомендуемых МО

   ```
   Ср. расстояние = Σ(distances) / count
   ```

3. **Средняя доступность** — средний % доступных коек
   ```
   Ср. доступность = Σ(availableBeds / totalBeds) / count
   ```

## ⚠️ Ограничения и нюансы

### 1. **Не учитывается специализация**

- Все МО равнозначны
- Не проверяется совместимость профилей коек
- Например: кардиология может быть рекомендована вместо педиатрии

### 2. **Расстояние "по прямой"**

- Не учитываются реальные дороги
- Фактическое время может быть больше

### 3. **Статические данные**

- Нет real-time обновлений
- Ситуация может измениться между обновлениями

### 4. **Не учитывается время суток**

- Средняя скорость 40 км/ч постоянна
- Не учитываются часы пик

### 5. **Нет проверки транспорта**

- Предполагается, что до всех МО можно добраться
- Не учитывается доступность общественного транспорта

## 🚀 Будущие улучшения

- [ ] Учет специализации и профиля коек
- [ ] Интеграция с реальными маршрутами (OSRM API)
- [ ] Учет времени суток и трафика
- [ ] Историческая аналитика перенаправлений
- [ ] Машинное обучение для оптимизации весов
- [ ] Проверка доступности транспорта
- [ ] Учет времени ожидания скорой помощи

## 💡 Рекомендации по использованию

1. **Всегда проверяйте расстояние и время** — убедитесь что перенаправление реалистично
2. **Смотрите на количество коек** — достаточно ли их для ваших пациентов
3. **Учитывайте специализацию вручную** — система этого не делает
4. **Координируйтесь с принимающим МО** — подтвердите готовность принять пациентов
5. **Используйте карту** — визуально оцените маршрут

---

**Версия:** 1.0  
**Последнее обновление:** Ноябрь 2025
